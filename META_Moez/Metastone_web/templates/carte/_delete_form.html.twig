<form method="post" action="{{ path('app_carte_delete', {'idCarte': carte.idCarte}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte.idCarte) }}">
    <button class="btn btn-danger">Delete</button>
</form>

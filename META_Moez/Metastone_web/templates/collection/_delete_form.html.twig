<form method="post" action="{{ path('app_collection_delete', {'idCollection': collection.idCollection}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection.idCollection) }}">
    <button class="btn">Delete</button>
</form>

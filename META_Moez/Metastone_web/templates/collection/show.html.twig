{% extends 'base_front.html.twig' %}

{% block title %}Collection{% endblock %}

{% block body %}
    <h1>Collection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCollection</th>
                <td>{{ collection.idCollection }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collection_index') }}">back to list</a>

    <a href="{{ path('app_collection_edit', {'idCollection': collection.idCollection}) }}">edit</a>

    {{ include('collection/_delete_form.html.twig') }}
{% endblock %}

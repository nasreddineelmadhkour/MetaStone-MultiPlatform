<form method="post" action="{{ path('app_tournoi_delete', {'idTournoi': tournoi.idTournoi}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.idTournoi) }}">
    <button class="btn">Delete</button>
</form>
